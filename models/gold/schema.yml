
version: 2

models:
  - name: dim_vinhome
    description: "Final Cleaned VinHome Data"
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: price
        tests:
          - not_null

  - name: dim_vinfast
    description: "Final Cleaned VinFast Data"
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: price
        tests:
          - not_null
